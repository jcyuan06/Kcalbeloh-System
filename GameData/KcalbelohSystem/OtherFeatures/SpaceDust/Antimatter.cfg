// Defines Antimatter definitions for stock planets
// KcalbelohSystem

// Rouqea
// =============
SPACEDUST_RESOURCE:NEEDS[SpaceDust]
{
  resourceName = Antimatter
  body = Rouqea
  RESOURCEBAND
  {
    name = RouqeaRing
    title = #LOC_SpaceDust_Band_LowBelt

    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 2.9E-29
    maxAbundance = 1.2E-28


    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 565000
    // minimum altitude for a distribution
    altLowerBound = 400000
    // peak of a distribution
    altPeak = 500000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 10
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear
    altitudeSquish = 0.9
    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 70
    // Minimum latitude for a distribution
    latLowerBound = -50
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 1
    // Falloff for latitude, can be Linear or None
    latFalloffType = Linear
  }

  RESOURCEBAND
  {
    name = RouqeaRingHigher
    title = #LOC_SpaceDust_Band_HighBelt

    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 2.9E-29
    maxAbundance = 1.2E-28

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 2800000
    // minimum altitude for a distribution
    altLowerBound = 2500000
    // peak of a distribution
    altPeak = 2700000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 10
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear
    altitudeSquish = 1

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 1
    // Falloff for latitude, can be Linear or None
    latFalloffType = Linear
  }

}

// Suluco
// =============
SPACEDUST_RESOURCE:NEEDS[SpaceDust]
{
  resourceName = Antimatter
  body = Suluco
  RESOURCEBAND
  {
    name = SulucoRing
    title = #LOC_SpaceDust_Band_LowBelt

    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 2.9E-30
    maxAbundance = 1.2E-29


    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 265000
    // minimum altitude for a distribution
    altLowerBound = 200000
    // peak of a distribution
    altPeak = 250000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 10
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear
    altitudeSquish = 0.9
    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 40
    // Minimum latitude for a distribution
    latLowerBound = -60
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 1
    // Falloff for latitude, can be Linear or None
    latFalloffType = Linear
  }

  RESOURCEBAND
  {
    name = SulucoRingHigher
    title = #LOC_SpaceDust_Band_HighBelt

    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 3.2E-30
    maxAbundance = 2.9E-29

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 2100000
    // minimum altitude for a distribution
    altLowerBound = 1900000
    // peak of a distribution
    altPeak = 2000000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 10
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear
    altitudeSquish = 1

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 50
    // Minimum latitude for a distribution
    latLowerBound = -50
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 1
    // Falloff for latitude, can be Linear or None
    latFalloffType = Linear
  }

}

// Efil
// =============
SPACEDUST_RESOURCE:NEEDS[SpaceDust]
{
  resourceName = Antimatter
  body = Efil
  RESOURCEBAND
  {
    name = EfilRing
    title = #LOC_SpaceDust_Band_LowBelt

    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 4.1E-30
    maxAbundance = 1.2E-29


    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 2080000
    // minimum altitude for a distribution
    altLowerBound = 150000
    // peak of a distribution
    altPeak = 1500000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 10
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear
    altitudeSquish = 0.9
    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 30
    // Minimum latitude for a distribution
    latLowerBound = -30
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 1
    // Falloff for latitude, can be Linear or None
    latFalloffType = Linear
  }

}

// Sedah
// =============

SPACEDUST_RESOURCE:NEEDS[SpaceDust]
{
  resourceName = Antimatter
  body = Sedah

  RESOURCEBAND
  {
    name = SedahExo
    title = #LOC_SpaceDust_Band_LowBelt

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 6.1E-31 //0.00000000000000000000000000000061
    maxAbundance = 4.21E-30 //0.00000000000000000000000000000421

    // Visuals Data
    // -------------
    countScale = 2
    rotateRate = 1
    
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 700000
    // minimum altitude for a distribution
    altLowerBound = 500000
    // peak of a distribution
    altPeak = 600000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 30
    // Minimum latitude for a distribution
    latLowerBound = -30
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 6
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }

  RESOURCEBAND
  {
    name = SedahAreaRing

    title = #LOC_SpaceDust_Band_Ring

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 1.1E-30 //0.0000000000000000000000000000011
    maxAbundance = 4.1E-30 //0.0000000000000000000000000000041

     // Visuals Data
    // -------------
    countScale = 20
    rotateRate = 0.25

    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 12500000
    // minimum altitude for a distribution
    altLowerBound = 10000000
    // peak of a distribution
    altPeak = 11000000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 2
    // Minimum latitude for a distribution
    latLowerBound = -2
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}

// Simetra
// =============

SPACEDUST_RESOURCE:NEEDS[SpaceDust]
{
  resourceName = Antimatter
  body = Simetra

  RESOURCEBAND
  {
    name = SimetraExo
    title = #LOC_SpaceDust_Band_LowBelt

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 6.1E-31 //0.00000000000000000000000000000061
    maxAbundance = 4.21E-30 //0.00000000000000000000000000000421

    // Visuals Data
    // -------------
    countScale = 2
    rotateRate = 1
    
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 600000
    // minimum altitude for a distribution
    altLowerBound = 400000
    // peak of a distribution
    altPeak = 500000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 25
    // Minimum latitude for a distribution
    latLowerBound = -25
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 6
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }

  RESOURCEBAND
  {
    name = SimetraAreaRing

    title = #LOC_SpaceDust_Band_Ring

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 1.1E-30 //0.0000000000000000000000000000011
    maxAbundance = 4.1E-30 //0.0000000000000000000000000000041

     // Visuals Data
    // -------------
    countScale = 20
    rotateRate = 0.25

    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 23000000
    // minimum altitude for a distribution
    altLowerBound = 21000000
    // peak of a distribution
    altPeak = 22000000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 2
    // Minimum latitude for a distribution
    latLowerBound = -2
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}

// Simeht
// =============

SPACEDUST_RESOURCE:NEEDS[SpaceDust]
{
  resourceName = Antimatter
  body = Simeht

  RESOURCEBAND
  {
    name = SimehtExo
    title = #LOC_SpaceDust_Band_LowBelt

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 5.1E-31 
    maxAbundance = 3.21E-30 
    // Visuals Data
    // -------------
    countScale = 2
    rotateRate = 1
    
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 500000
    // minimum altitude for a distribution
    altLowerBound = 300000
    // peak of a distribution
    altPeak = 400000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 20
    // Minimum latitude for a distribution
    latLowerBound = -20
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 6
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }

  RESOURCEBAND
  {
    name = SimehtAreaRing

    title = #LOC_SpaceDust_Band_Ring

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 1.0E-30 //0.0000000000000000000000000000011
    maxAbundance = 3.1E-30 //0.0000000000000000000000000000041
     // Visuals Data
    // -------------
    countScale = 20
    rotateRate = 0.25

    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 21000000
    // minimum altitude for a distribution
    altLowerBound = 20000000
    // peak of a distribution
    altPeak = 20100000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 2
    // Minimum latitude for a distribution
    latLowerBound = -2
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}

// Aciore
// =============

SPACEDUST_RESOURCE:NEEDS[SpaceDust]
{
  resourceName = Antimatter
  body = Aciore

  RESOURCEBAND
  {
    name = AcioreExo
    title = #LOC_SpaceDust_Band_LowBelt

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 7.61E-30
    maxAbundance = 1.521E-29
    // Visuals Data
    // -------------
    countScale = 2
    rotateRate = 1
    
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 900000
    // minimum altitude for a distribution
    altLowerBound = 700000
    // peak of a distribution
    altPeak = 825000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 40
    // Minimum latitude for a distribution
    latLowerBound = -40
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 6
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }

  RESOURCEBAND
  {
    name = AcioreAreaRing

    title = #LOC_SpaceDust_Band_Ring

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 9.1E-30
    maxAbundance = 1.01E-29
    // Visuals Data
    // -------------
    countScale = 20
    rotateRate = 0.25

    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 13165000 //30000000
    // minimum altitude for a distribution
    altLowerBound = 4405000 //29000000
    // peak of a distribution
    altPeak = 8650000 //29550000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 2
    // Minimum latitude for a distribution
    latLowerBound = -2
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}

// Kcalbeloh
// =============

SPACEDUST_RESOURCE:NEEDS[SpaceDust]
{
  resourceName = Antimatter
  body = Kcalbeloh

  RESOURCEBAND
  {
    name = KcalbelohExo
    title = #LOC_SpaceDust_Band_LowBelt

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 7.61E-27
    maxAbundance = 1.521E-26

    // Visuals Data
    // -------------
    countScale = 2
    rotateRate = 1
    
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 12000000000
    // minimum altitude for a distribution
    altLowerBound = 5000000000
    // peak of a distribution
    altPeak = 10000000000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 30
    // Minimum latitude for a distribution
    latLowerBound = -30
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 6
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }

  RESOURCEBAND
  {
    name = KcalbelohAreaRing

    title = #LOC_SpaceDust_Band_Ring

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 9.1E-28
    maxAbundance = 1.01E-27

     // Visuals Data
    // -------------
    countScale = 20
    rotateRate = 0.25

    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 21000000000
    // minimum altitude for a distribution
    altLowerBound = 7000000000
    // peak of a distribution
    altPeak = 16000000000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 3
    // Minimum latitude for a distribution
    latLowerBound = -3
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}